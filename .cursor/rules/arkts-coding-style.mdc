---
description: HarmonyOS ArkTS 编码风格规范（参考官方 ArkTS Coding Style Guide）
globs: **/*.ets
alwaysApply: false
---

# ArkTS 编码风格规范

本规则参考华为官方 ArkTS 编码风格指南，约束本项目 `.ets` 文件的命名、格式与结构，提升可读性与一致性。

## 1. 命名规范

- **文件命名**
  - 页面/组件：`PascalCase.ets`，如 `UserProfilePage.ets`。
  - 工具/接口：`CamelCase.ets` 或有前缀，如 `ApiClient.ets`、`ApiTypes.ets`。
- **类型与接口**
  - 接口、类型、类使用 `PascalCase`：`UserInfoData`、`OrderItem`。
  - 响应类型推荐统一为 `XXXResp`，数据结构为 `XXXData`。
- **变量与函数**
  - 使用 `camelCase`，名称体现语义：`getUserInfo`、`orderListData`。
  - 布尔变量以 `is/has/can/should` 开头：`isSigned`、`hasError`。

## 2. 代码格式

- **缩进与空格**
  - 使用 2 或 4 个空格缩进（与工程设置保持一致），禁止 Tab / 空格混用。
  - 关键字与括号之间保留一个空格：`if (condition) {`。
- **换行与最大行宽**
  - 建议单行长度不超过 120 列，过长时按语义位置换行（参数列表、对象属性等）。
- **导入顺序**
  - 先导入系统/框架模块（如 `@ohos.*`），再导入项目内模块，组内按字母序排列。

## 3. 结构与注释

- **模块结构**
  - 导入 → 类型定义 → 内部常量 → 工具函数 → 对外导出函数/类，结构清晰分组。
- **注释**
  - 只为复杂逻辑、边界条件、业务约定写注释，不为显而易见的代码加解释性注释。
  - 推荐使用简短的单行注释或必要的块注释，保持注释与代码同步。

## 4. 函数与复杂度

- 函数职责要单一，避免超长函数：
  - 一般函数建议控制在 50 行以内，超过则考虑拆分。
  - 公共逻辑抽到独立工具函数或模块中复用。
- 参数个数不宜过多（>4 时考虑封装为对象类型）。

## 5. 错误处理与日志

- 网络调用、文件操作等失败风险点必须有错误处理：
  - 不允许空 `catch`；至少记录日志或返回明确错误态。
  - 在 ArkTS 中优先使用框架提供的日志手段（如 `hilog` 等），避免 `console.log` 滥用。

